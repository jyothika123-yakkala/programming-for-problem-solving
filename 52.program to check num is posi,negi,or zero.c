#include<stdio.h>
int main()
{
	int num;
	printf("\n enter a number:");
	scanf("%d",&num);
	if(num>0)
	printf("\n number is positive");
	if(num<0)
	printf("\n num is negitive");
	if(num = 0)
	printf("\n num is zero");
	return(0);
}
